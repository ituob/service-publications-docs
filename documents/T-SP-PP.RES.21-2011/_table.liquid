== {{ metadata.title[lang] }}

{% if lang == "en" %}
*Note from TSB*
{% elsif lang == "fr" %}
*Note du TSB*
{% elsif lang == "es" %}
*Nota de la TSB*
{% endif %}

{{ metadata.locale | where: "key", "note" | map: lang | first }}

{% assign half_size_data = data.size | divided_by: 2 %}

[cols="4",options="unnumbered"]
|===
{% for n in (0..half_size_data) -%}
    {%- assign i = n -%}
    {%- assign j = i | plus: half_size_data | minus: 1 -%}
    {%- assign column_one = data[i] -%}
    {%- assign column_two = data[j] -%}
    | {{ column_one.number }} | {{ column_one.geographic_area[lang] }}
    | {{ column_two.number }} | {{ column_two.geographic_area[lang] }}
{%- endfor %}
|===

{% if lang == "en" %}
All the countries/geographical areas which prohibit the practice of "Call-Back" should inform ITU accordingly by sending e-mail to the following address: mailto:tsbtson@itu.int[]
{% elsif lang == "fr" %}
Tous les pays/zones géographiques interdisant la pratique du système de rappel devraient aviser dûment l'UIT par e-mail à l'adresse suivante: mailto:tsbtson@itu.int[]
{% elsif lang == "es" %}
Se solicita a todos los países/zonas geográficas que prohíben las comunicaciones por intermediario (Call-Back) de notificarlo debidamente a la UIT a la siguiente dirección de correo electrónico: mailto:tsbtson@itu.int[]
{% endif %}